PREFIX ex: <http://example.org/mobility#>

SELECT (STRAFTER(STR(?zone), "#") AS ?zoneName)
       (AVG(?delay) AS ?avgDelay)
WHERE {
  ?bus a ex:Bus ;
       ex:locatedIn ?zone ;
       ex:delayMinutes ?delay .
}
GROUP BY ?zone
ORDER BY DESC(?avgDelay)
